import React, { Fragment } from "react";


const Content = props => {
    return (
        <Fragment>
            <p>Header</p>
            {/* Header / Main pages / Footer*/}
        </Fragment>
    );
};

export default Content;