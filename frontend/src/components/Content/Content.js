import React, { Fragment } from "react";

import Header from './Header/Header';

const Content = props => {
    return (
        <Fragment>
            <Header />
            {/* Main pages / Footer*/}
        </Fragment>
    );
};

export default Content;