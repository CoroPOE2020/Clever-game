import React, { Fragment } from "react";

import Menu from './Menu/Menu';

const Header = props => {
    return (
        <Fragment>
            <Menu />
        </Fragment>
    );
};

export default Header;